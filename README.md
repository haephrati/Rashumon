# Rashumon

GitHub Pages site for Rashumon, the multilingual Amiga word processor Michael Haephrati developed at HarmonySoft.

## Unit tests

`Tests/index.test.js` reads `index.html` and `params.json` and checks the product name and Amiga tagline. Run:

```
node Tests\index.test.js
```
